# Subtask 1 Plan: Define `backend-agent` Role

- **Owner:** `architect-agent`
- **Scope:** Create a new constitution file that defines the `backend-agent`.
- **Files to Create:**
    - `.agent/rules/role/backend-agent.md`
- **Acceptance Criteria:**
    - The file must be created.
    - It must define the agent's responsibility as managing business logic and data persistence.
    - It must explicitly state that the agent's file modification rights are restricted to `backend/` directories within modules.
