# Subtask 2 Plan: Define `background-agent` Role

- **Owner:** `architect-agent`
- **Scope:** Create a new constitution file that defines the `background-agent`.
- **Files to Create:**
    - `.agent/rules/role/background-agent.md`
- **Acceptance Criteria:**
    - The file must be created.
    - It must define the agent's responsibility as managing the view lifecycle, routing messages, and orchestrating the different layers of a module.
    - It must explicitly state that the agent's file modification rights are restricted to `background/` directories within modules.
