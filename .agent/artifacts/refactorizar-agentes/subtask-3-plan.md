# Subtask 3 Plan: Define `view-agent` Role

- **Owner:** `architect-agent`
- **Scope:** Create a new constitution file that defines the `view-agent`.
- **Files to Create:**
    - `.agent/rules/role/view-agent.md`
- **Acceptance Criteria:**
    - The file must be created.
    - It must define the agent's responsibility as implementing and modifying Lit-based UI components.
    - It must explicitly state that the agent's file modification rights are restricted to `view/` directories within modules.
