
import { css } from 'lit';

export const styles = css`
  :host {
    display: block;
    padding: 20px;
    font-family: var(--vscode-font-family);
    color: var(--vscode-foreground);
  }

  h1 {
    font-size: 1.5em;
    margin-bottom: 1em;
  }

  p {
    margin-bottom: 1em;
    line-height: 1.5;
  }
`;
